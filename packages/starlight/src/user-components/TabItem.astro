---
import { TabItemTagname } from './tabs-processor';
import type { StarlightIcon } from '../components-internals/Icons';

interface Props {
	icon?: StarlightIcon;
	label: string;
}

const { icon, label } = Astro.props;

if (!label) {
	throw new Error('Missing prop `label` on `<TabItem>` component.');
}
---

<TabItemTagname data-label={label} data-icon={icon}>
	<slot />
</TabItemTagname>
