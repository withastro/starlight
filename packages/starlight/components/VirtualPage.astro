---
import { generateVirtualRouteData, type VirtualPageProps as Props } from '../utils/route-data';
import Page from './Page.astro';

export { makeVirtualStaticPaths } from '../utils/virtual-page';

export type VirtualPageProps = Props;
---

<Page {...generateVirtualRouteData({ props: Astro.props, url: Astro.url })}>
	<slot />
</Page>
