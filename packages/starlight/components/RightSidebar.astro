---
import type { StarlightRouteData as Props } from '../utils/route-data';

import {
	RightSidebarPanel,
	TableOfContents,
	MobileTableOfContents,
} from 'virtual:starlight/components';
---

{
	Astro.props.toc && (
		<>
			<MobileTableOfContents {...Astro.props} />
			<RightSidebarPanel {...Astro.props}>
				<TableOfContents {...Astro.props} />
			</RightSidebarPanel>
		</>
	)
}
